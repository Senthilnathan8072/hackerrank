#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
      int a, b, c;
    std::cin >> a >> b >> c;  
    int sum = a + b + c;
    std::cout << sum << std::endl;
    return 0;
}
